# Rýchly návod

1) Spustiť:
```
docker compose up --build
```

2) Formulár bude na `http://localhost:8080/` a phpMyAdmin na `http://localhost:8081`
   (host: `db`, user: `student`, pass: `student`).

